/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // basePath: '/next-portfolio',
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
